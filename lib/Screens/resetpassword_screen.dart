import 'package:flutter/material.dart';
class ResetPasswordTwo extends StatelessWidget {
  const ResetPasswordTwo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        width: 100,
        height: 100,
      ),
    );
  }
}
