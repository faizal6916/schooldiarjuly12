import 'package:flutter/material.dart';

class CircularMainScreen extends StatelessWidget {
  const CircularMainScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Circular'),
    );
  }
}
